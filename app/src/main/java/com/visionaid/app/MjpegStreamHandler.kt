package com.jk.kukuapplication

// Handles MJPEG camera stream from device to backend or local preview
class MjpegStreamHandler {
    // TODO: Implement MJPEG streaming logic + frame capture for backend API calls
    fun startStreaming() {
        // Use CameraX or old Camera API + send frames to FastAPI endpoint
    }
}
